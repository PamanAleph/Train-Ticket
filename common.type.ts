export interface TrainTicket {
  TicketId: number;
  TicketTrain: string;
  TicketFromCity: string;
  TicketToCity: string;
  TicketClass: string;
  TicketPrice: string;
  TicketDate: string;
  TicketDeparture: string;
  TicketArrived: string;
}
